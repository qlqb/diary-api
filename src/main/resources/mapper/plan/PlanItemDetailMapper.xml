<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jungwoo.project.memo.plan.PlanItemDetailMapper">

    <resultMap id="detailResultMap" type="com.jungwoo.project.memo.plan.domain.PlanItemDetail">
        <id     property="detailId"        column="detail_id"/>
        <result property="userId"          column="user_id"/>
        <result property="proposalItemId"  column="proposal_item_id"/>
        <result property="evidenceVersion" column="evidence_version"/>
        <result property="stepsJson"       column="steps_json"/>
        <result property="userText"        column="user_text"/>
        <result property="status"          column="status"/>
        <result property="model"           column="model"/>
        <result property="createdAt"       column="created_at"/>
        <result property="updatedAt"       column="updated_at"/>
    </resultMap>

    <insert id="insertIgnore" useGeneratedKeys="true" keyProperty="detailId">
        INSERT IGNORE INTO plan_item_details (user_id, proposal_item_id, evidence_version, steps_json, user_text, status, model)
        VALUES (#{userId}, #{proposalItemId}, #{evidenceVersion}, #{stepsJson}, #{userText}, 'CURRENT', #{model})
    </insert>

    <select id="findByItemAndVersion" resultMap="detailResultMap">
        SELECT * FROM plan_item_details
        WHERE proposal_item_id = #{proposalItemId} AND evidence_version = #{evidenceVersion} AND user_id = #{userId}
    </select>

    <select id="findByIdAndUserId" resultMap="detailResultMap">
        SELECT * FROM plan_item_details WHERE detail_id = #{detailId} AND user_id = #{userId}
    </select>

    <select id="findByItem" resultMap="detailResultMap">
        SELECT * FROM plan_item_details
        WHERE proposal_item_id = #{proposalItemId} AND user_id = #{userId}
        ORDER BY created_at DESC, detail_id DESC
    </select>

    <update id="staleOthers">
        UPDATE plan_item_details SET status = 'STALE'
         WHERE proposal_item_id = #{proposalItemId} AND user_id = #{userId}
           AND evidence_version &lt;&gt; #{keepVersion} AND status = 'CURRENT'
    </update>

    <update id="updateUserText">
        UPDATE plan_item_details SET user_text = #{userText}
         WHERE detail_id = #{detailId} AND user_id = #{userId}
    </update>

</mapper>
