<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jungwoo.project.memo.material.CourseMaterialAnalysisMapper">

    <resultMap id="analysisResultMap" type="com.jungwoo.project.memo.material.domain.CourseMaterialAnalysis">
        <id     property="analysisId"     column="analysis_id"/>
        <result property="userId"         column="user_id"/>
        <result property="courseId"       column="course_id"/>
        <result property="materialId"     column="material_id"/>
        <result property="status"         column="status"/>
        <result property="analysisJson"   column="analysis_json"/>
        <result property="editedJson"     column="edited_json"/>
        <result property="model"          column="model"/>
        <result property="failureReason"  column="failure_reason"/>
        <result property="createdAt"      column="created_at"/>
        <result property="appliedAt"      column="applied_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="analysisId">
        INSERT INTO course_material_analyses (
            user_id, course_id, material_id, status, analysis_json, edited_json, model, failure_reason
        ) VALUES (
            #{userId}, #{courseId}, #{materialId}, #{status}, #{analysisJson}, #{editedJson}, #{model}, #{failureReason}
        )
    </insert>

    <select id="findByIdAndUserId" resultMap="analysisResultMap">
        SELECT * FROM course_material_analyses WHERE analysis_id = #{analysisId} AND user_id = #{userId}
    </select>

    <select id="findByMaterialIdAndUserId" resultMap="analysisResultMap">
        SELECT * FROM course_material_analyses
        WHERE material_id = #{materialId} AND user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <!--
      프로젝트 맥락 조회. WHERE에 course_id를 더하는 것만으로는 부족해서 서비스에서 링크
      검증을 먼저 한다 — 연결되지 않은 자료면 빈 배열이 아니라 MATERIAL_NOT_LINKED_TO_COURSE다.
    -->
    <select id="findByMaterialIdAndCourseIdAndUserId" resultMap="analysisResultMap">
        SELECT * FROM course_material_analyses
        WHERE material_id = #{materialId} AND course_id = #{courseId} AND user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <update id="updateEditedJson">
        UPDATE course_material_analyses SET edited_json = #{editedJson} WHERE analysis_id = #{analysisId}
    </update>

    <update id="updateStatus">
        UPDATE course_material_analyses
        SET status = #{status}, applied_at = #{appliedAt}
        WHERE analysis_id = #{analysisId}
    </update>

</mapper>
