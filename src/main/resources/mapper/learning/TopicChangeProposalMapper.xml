<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jungwoo.project.memo.learning.TopicChangeProposalMapper">

    <resultMap id="proposalResultMap" type="com.jungwoo.project.memo.learning.domain.TopicChangeProposal">
        <id     property="proposalId"      column="proposal_id"/>
        <result property="userId"          column="user_id"/>
        <result property="courseId"        column="course_id"/>
        <result property="materialId"      column="material_id"/>
        <result property="jobId"           column="job_id"/>
        <result property="fileHash"        column="file_hash"/>
        <result property="baseTreeVersion" column="base_tree_version"/>
        <result property="status"          column="status"/>
        <result property="summaryJson"     column="summary_json"/>
        <result property="opsJson"         column="ops_json"/>
        <result property="appliedOpsJson"  column="applied_ops_json"/>
        <result property="model"           column="model"/>
        <result property="createdAt"       column="created_at"/>
        <result property="resolvedAt"      column="resolved_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="proposalId">
        INSERT INTO topic_change_proposals (
            user_id, course_id, material_id, job_id, file_hash, base_tree_version, status, summary_json, ops_json, model
        ) VALUES (
            #{userId}, #{courseId}, #{materialId}, #{jobId}, #{fileHash}, #{baseTreeVersion}, #{status}, #{summaryJson}, #{opsJson}, #{model}
        )
    </insert>

    <select id="findByIdAndUserId" resultMap="proposalResultMap">
        SELECT * FROM topic_change_proposals WHERE proposal_id = #{proposalId} AND user_id = #{userId}
    </select>

    <select id="findByIdAndUserIdForUpdate" resultMap="proposalResultMap">
        SELECT * FROM topic_change_proposals WHERE proposal_id = #{proposalId} AND user_id = #{userId} FOR UPDATE
    </select>

    <select id="findOpenByCourseId" resultMap="proposalResultMap">
        SELECT * FROM topic_change_proposals
        WHERE course_id = #{courseId} AND user_id = #{userId} AND status = 'PROPOSED'
        ORDER BY created_at DESC, proposal_id DESC
    </select>

    <select id="findByCourseId" resultMap="proposalResultMap">
        SELECT * FROM topic_change_proposals
        WHERE course_id = #{courseId} AND user_id = #{userId}
        ORDER BY created_at DESC, proposal_id DESC
        LIMIT #{limit}
    </select>

    <select id="findOpenByMaterialAndCourse" resultMap="proposalResultMap">
        SELECT * FROM topic_change_proposals
        WHERE material_id = #{materialId} AND course_id = #{courseId} AND user_id = #{userId} AND status = 'PROPOSED'
        LIMIT 1
    </select>

    <update id="updateStatus">
        UPDATE topic_change_proposals
           SET status = #{status}, applied_ops_json = #{appliedOpsJson}, resolved_at = #{resolvedAt}
         WHERE proposal_id = #{proposalId} AND user_id = #{userId}
    </update>

    <update id="staleOpen">
        UPDATE topic_change_proposals
           SET status = 'STALE', resolved_at = #{resolvedAt}
         WHERE material_id = #{materialId} AND user_id = #{userId} AND status = 'PROPOSED'
         <if test="courseId != null">AND course_id = #{courseId}</if>
    </update>

</mapper>
