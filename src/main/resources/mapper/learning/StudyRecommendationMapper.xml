<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jungwoo.project.memo.learning.StudyRecommendationMapper">

    <resultMap id="studyRecommendationResultMap" type="com.jungwoo.project.memo.learning.domain.StudyRecommendation">
        <id     property="recommendationId"        column="recommendation_id"/>
        <result property="userId"                  column="user_id"/>
        <result property="courseId"                column="course_id"/>
        <result property="topicId"                 column="topic_id"/>
        <result property="proposalId"               column="proposal_id"/>
        <result property="activityType"             column="activity_type"/>
        <result property="recommendedMinutesMin"    column="recommended_minutes_min"/>
        <result property="recommendedMinutesIdeal"  column="recommended_minutes_ideal"/>
        <result property="priority"                 column="priority"/>
        <result property="reason"                   column="reason"/>
        <result property="status"                   column="status"/>
        <result property="sourceConversationId"     column="source_conversation_id"/>
        <result property="sourceMessageId"          column="source_message_id"/>
        <result property="createdAt"                column="created_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="recommendationId">
        INSERT INTO study_recommendations (
            user_id, course_id, topic_id, activity_type, recommended_minutes_min,
            recommended_minutes_ideal, priority, reason, status, source_conversation_id, source_message_id
        ) VALUES (
            #{userId}, #{courseId}, #{topicId}, #{activityType}, #{recommendedMinutesMin},
            #{recommendedMinutesIdeal}, #{priority}, #{reason}, #{status}, #{sourceConversationId}, #{sourceMessageId}
        )
    </insert>

    <select id="findByIdAndUserId" resultMap="studyRecommendationResultMap">
        SELECT * FROM study_recommendations WHERE recommendation_id = #{recommendationId} AND user_id = #{userId}
    </select>

    <select id="findRecentByUserId" resultMap="studyRecommendationResultMap">
        SELECT * FROM study_recommendations
        WHERE user_id = #{userId}
        ORDER BY created_at DESC, recommendation_id DESC
        LIMIT #{limit}
    </select>

    <update id="updateStatus">
        UPDATE study_recommendations SET status = #{status} WHERE recommendation_id = #{recommendationId}
    </update>

    <update id="attachProposal">
        UPDATE study_recommendations
        SET proposal_id = #{proposalId}, status = #{status}
        WHERE recommendation_id = #{recommendationId}
    </update>

    <select id="findByProposalIdAndUserId" resultMap="studyRecommendationResultMap">
        SELECT * FROM study_recommendations WHERE proposal_id = #{proposalId} AND user_id = #{userId}
    </select>

</mapper>
