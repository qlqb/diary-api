package com.jungwoo.project.memo.plan.domain;

/**
 * 이 취급을 모델이 낸 대로 두지 않고 누가 바꿨는가.
 *
 * <p>지금 값은 하나뿐이지만 enum으로 두는 이유는, 화면이 "왜 이렇게 됐는지"를 말할 때
 * "모델이 그렇게 봤다"와 "당신이 표시해서 그렇게 됐다"를 구분해야 하기 때문이다. boolean으로
 * 두면 나중에 다른 조정 주체(사용자 직접 편집 등)가 생겼을 때 뜻이 무너진다.
 */
public enum AdjustedBy {

    /** 서버가 사용자의 표식이나 근거 등급에 따라 되돌렸다. 모델의 판단이 아니다. */
    SERVER
}
