# findings 2026-09-08

handoff: "상담 모델 상향·분리 + 진행 중 요청 상태(draft) 도입". 브랜치 `dev-playground`.

## 사전 점검

- `spring.ai.openai.chat.model`을 `@Value`로 읽는 클래스: `AiConversationService`,
  `PeriodPlanDraftGenerator`, `PlanItemService`, `PlanJudgmentService`, `LearningConversationService`,
  `LearningRecommendationService`, `MaterialLinkProposalService`, `MaterialAnalysisService`,
  `PlanningAgentService` (9개. 문서의 5개보다 많다). 전부 하드코딩 기본값 `gpt-5-mini`였고
  `modelName`을 `aiUsageLimitService.record(...)`로 `ai_usage_logs.model`에 남긴다 → 기능별 실제
  모델이 남는 구조라 상담 경로만 값을 바꾸면 그대로 구분된다.
- `ai.schedule-import.model`: `ScheduleImageVisionClient`가 `@Value("${ai.schedule-import.model:}")`로
  읽고 비어 있으면 옵션을 얹지 않아 자동 설정의 전역 모델을 쓴다. Stage A는 이 패턴을 복제했다.
- `OpenAiConsultationClient.streamTurn`: 2인자(default → 3인자 null) / 3인자(`maxCompletionTokens`).
  모델 문자열은 어디서도 주입하지 않았다 — Spring AI 자동 설정의 기본 옵션(전역 모델)이 그대로
  쓰였다. 상담 모델을 따로 두려면 호출별 `OpenAiChatOptions.model(...)`이 필요해 4인자 오버로드를
  추가했다(아래 Stage A).
- `AiConversationService` 실제 위치(문서의 :405/:786/:903과 다르다):
  - `[최근 대화]` 블록 조립: `ContextSnapshotService.buildContextBlock` 호출부 :371 부근
    (`contextBudgetChars = maxChars - currentMessageChars - workspaceBlock.length()`).
  - ASK 시 ASSISTANT 저장: `resolveAutoTurn`의 `ASK_CLARIFICATION` 분기가 `clarifyingQuestion`을
    reply로 삼아 CHAT으로 저장(:713 부근). 저장 자체는 `AiTurnLifecycleService.completeTurnSuccess`.
  - OFFER에 기간·강도 없을 때 고정 문구 재질문: `resolveOffer`(:835 부근)의 `PERIOD_QUESTION` /
    `INTENSITY_QUESTION`. **이미 `proposalPurpose == PERIOD_PLAN`일 때만 도달한다.** Stage B는
    여기에 "OPEN draft가 있고 그중 `CREATE_PERIOD_PLAN`이 없으면 발동하지 않는다"는 조건을 더한다.
- `ContextSnapshotService.buildContextBlock`: 배분 0.50/0.35/0.15, `SEPARATOR_RESERVE_CHARS=2`.
  문서와 같다. Stage B의 draft 블록은 이 서비스 밖에서 붙이고 호출부 예산만 줄인다.
- `AiWorkspaceContextBuilder`: 시간표는 `routineService.list(userId)` → `RoutineResponse`
  (`courseId != null && !ended`)를 코스별로 묶어 `"수업 화 14:00~17:00"` **문자열**로 렌더링한다.
  원천은 구조화돼 있다(`RoutineResponse.daysOfWeek: Set<DayOfWeek>`, `startTime/endTime: LocalTime`,
  `effectiveFrom/effectiveUntil: LocalDate`). Stage B resolver는 이 `RoutineResponse`를 입력으로 쓴다.
- `ScheduleSuggestionService`: 생성 진입점 `createFromSuggestions(userId, conversationId,
  sourceMessageId, List<ScheduleSuggestion>)` (턴당 최대 5개, 모델 payload) 와 `createFromImport`
  (이미지, 최대 31개, 서버 payload). `proposed_payload`는 `RoutineSaveRequest` /
  `CommitmentCreateRequest` 필드명 그대로이고, **읽을 때 `FAIL_ON_UNKNOWN_PROPERTIES`로 모르는
  키를 거절한다**(커밋 a43e12a). → 문서 B-6의 "승인 경로가 두 키를 모르면 무시되어야 한다"는
  전제가 성립하지 않는다. Stage B 결정 로그 참고.
- `RoutineSaveRequest`: `courseId, title, location, daysOfWeek: Set<DayOfWeek>, startTime/endTime:
  LocalTime, effectiveFrom: LocalDate(필수), effectiveUntil: LocalDate(nullable)`. **종료일 필드가
  있다** → §7 fail-closed 규칙은 발동하지 않는다.
- 프로젝트(`courses`)에는 학기 종료일 컬럼이 없다. 수업 루틴의 `effective_until`이 종강일이다
  (`2026-08-31-routines.sql`: "수업은 만들 때 종강일을 채우고"). 로컬 DB 실데이터도 코스 루틴 7건
  전부 `2026-12-11`. Stage B `SemesterEndResolver`는 이 값을 쓴다(결정 로그).
- 근무 항목 분류: `one_off_commitments`에 분류 컬럼 없음(`title/start_at/end_at/location_text/
  source_type`). → §7대로 제목 키워드 상수(`근무|알바|출근`) 방식.
- 백엔드 테스트 기준선: `./gradlew test` **816 tests, 0 failures, 1 skipped** (`chmod +x gradlew`
  불필요, exit 0).
- **`proposal.ready` 턴의 ASSISTANT 텍스트 저장·렌더링**: 된다. `AiPanel.jsx`는 `message.delta`를
  스트리밍 말풍선에 붙이고, `schedule.suggestions.ready`는 별도 카드 목록에 추가하며,
  `message.completed`에서 `data.reply`로 말풍선 본문을 확정한다. 서버는 `completeTurnSuccess`가
  reply를 ASSISTANT 행으로 저장한다. → **PROPOSE + 남은 질문 한 줄(mixed) 가능.** SSE/프론트
  구조 변경 없음.
- `OPENAI_API_KEY` 환경변수: 프로세스·사용자·머신 스코프 모두 **없음**. 단
  `src/main/resources/application-local.properties`(git 미추적)에 `spring.ai.openai.api-key`가
  있어 로컬 서버는 AI가 설정된 상태로 뜬다. 이 키로 `/v1/models`만 읽어 모델 문자열을 확인했고,
  실호출 항목(A-4, B-9)은 로컬 서버를 띄워 진행했다(결과는 각 Stage 절).

## Stage A

- 사용 모델: consultation=`gpt-5.6-terra`, global=`gpt-5.6-luna` (`/v1/models` 확인: **있음**.
  응답 125개 중 gpt-5 계열에 `gpt-5.6-luna`, `gpt-5.6-sol`, `gpt-5.6-terra`, `gpt-5.4`,
  `gpt-5.4-mini`, `gpt-5-mini` 등이 있었다. 1순위를 그대로 썼다.)
- 구현: `application.properties`에 `ai.consultation.model=${AI_CONSULTATION_MODEL:gpt-5.6-terra}`,
  전역 기본값 `gpt-5.6-luna`. 코드 내 `@Value` 하드코딩 기본값 9곳 `gpt-5.6-luna`로.
  `AiConsultationClient`에 4인자 오버로드 `streamTurn(system, user, maxCompletionTokens, model)`
  추가(2/3인자는 default로 위임, 기존 동작 동일). `AiConversationService`만
  `resolveConsultationModel()`(전용 값 → 비면 전역)로 호출·사용량 기록 양쪽에 같은 모델을 쓴다.
- 파라미터 거부: 없음. 실호출 39턴 전부 `finishReason=STOP`, 서버 로그에 400/스트리밍 실패 없음.
  `ai_usage_logs`에 `model=gpt-5.6-terra, feature=AI_CONSULTATION, SUCCESS` 39건이 남았다.
- 테스트: **818/818 통과** (기준선 816 + 신규 2: 모델 fallback, 호출·사용량 기록 모델 일치), skipped 1(기존).
- 베이스라인(terra, `--seed` 픽스처: 화 14:00 / 수·목·금 10:00 첫 수업, 종강 12/11, 근무 4건. 대화 866~868):

  | run | 턴 수 | ASK 수 | OFFER/PROPOSAL 첫 등장 턴 | 마지막 응답 요지 |
  |---|---|---|---|---|
  | 1 | 13 | 10 | 1 (COMMITMENT 1건) | "12월 11일에 대해 어떤 일정이나 계획을 생각하고 계신가요?" |
  | 2 | 13 | 9 | 1 (COMMITMENT 1건, 2턴에 또 1건) | "12월 11일을 말씀하신 거군요. 어떤 일정이나 계획과 관련된 날짜인지 알려주시면 함께 정리할게요." |
  | 3 | 13 | 9 | 1 (ROUTINE 화 13:00~14:00 1건 + COMMITMENT 1건, 2턴에 같은 것 반복) | "날짜를 적어주셨네요. 2026년 12월 11일과 관련해 무엇을 정리해볼까요?" |

  턴별 분류(ASK=물음표/선택지, PROPOSAL=일정 후보 이벤트):
  run1 `P A A A A C C A A A A A A`, run2 `P P A A A A A A A C A A C`, run3 `P P A A A A A C C A A A A`.
  모델을 올려도 13턴째에는 "12월 11일"이 무엇의 날짜인지조차 잃는다 — 서버가 확정값을 들고
  있지 않아 매 턴 [최근 대화] 6개로 복원하는 구조가 원인이라는 진단이 그대로 재현됐다.
  첫 실행에서 스크립트가 SSE 바이트를 1바이트씩 decode해 한글이 깨졌다(표의 "요지"는 DB의
  ASSISTANT 행에서 가져왔다). 스크립트는 증분 디코더로 고쳤다.
- 결정: 재현 스크립트는 `.py`로 두었다 / 이유: 이 머신의 Git Bash는 한글을 CP949로 넘겨 요청
  본문이 깨진다(서버 500). 파이썬이 UTF-8을 명시할 수 있다 / 대안: `.sh`+`curl`은 본문을 파일로
  써야 해 스크립트가 둘로 갈린다.

## Stage B

- 결정 로그 (§0-2 형식):
  - 결정: `SemesterEndResolver`는 아직 끝나지 않은 수업 루틴(courseId 있음)의 `effective_until` 중 가장
    늦은 날을 `DB / ACTIVE_SEMESTER_END`로 쓴다 / 이유: `courses`에 종료일 컬럼이 없고, 루틴 DDL이
    수업 루틴의 effective_until을 종강일로 정의한다("수업은 만들 때 종강일을 채우고"). 저장된 사실이지
    추측이 아니다. 종강일이 하나도 없으면 empty → endDate missing(묻는다) / 대안: §7 "프로젝트 종료일
    필드가 없음 → missing 유지" — 그러면 실데이터(코스 루틴 전부 12/11)에서도 매번 종료일을 묻게 된다.
  - 결정: payload의 `fieldNotes`/`assumedFields`는 저장 컬럼에 그대로 두되, `ScheduleSuggestionService`가
    DTO로 읽기 직전(`withoutAnnotations`)과 화면 응답(`toResponse`)에서 떼어낸다. 응답에는
    `ScheduleSuggestionResponse.fieldNotes/assumedFields`로 따로 내린다 / 이유: 승인 경로는
    `FAIL_ON_UNKNOWN_PROPERTIES`로 모르는 키를 거절한다(a43e12a). 문서 B-6의 "모르면 무시" 전제가
    성립하지 않아 그대로 넣으면 [적용]이 503으로 깨진다. 카드가 [적용] 때 되돌려 보내는 payload는 DTO
    그대로여야 하므로 응답 payload에서도 뺐다 / 대안: 안내를 DB에 저장하지 않고 SSE에만 싣기 —
    새로고침 후 안내가 사라진다.
  - 결정: draft 승격은 새 메서드 `ScheduleSuggestionService.createFromDraft`(서버 payload, 상한 31,
    `PayloadSource.SERVER` → 실패 시 503+error 로그)로 한다 / 이유: `createFromSuggestions`는 모델
    payload용이라 턴당 5개 상한이 있는데 첫 수업 요일 4건 + 근무 2주치가 정상 범위다. 승인/적용
    경로(`apply/applyBatch/dismiss`)는 수정하지 않았다 / 대안: 상한을 올리기 — 모델 후보 폭주를 막는
    숫자를 건드리게 된다.
  - 결정: `CREATE_PERIOD_PLAN` draft가 READY면 같은 턴에서 `PlanDraftService`를 부르지 않고 기존 기간
    계획 OFFER(`OfferAction.createPeriodPlan`, CREATE_PERIOD_PLAN 버튼)로 넘기고 draft를 PROMOTED로
    기록한다 / 이유: `PlanDraftService.generate`는 모델을 부른다. 같은 턴에서 부르면 "사용자 메시지
    1개당 OpenAI 호출 1회"가 깨지고 트랜잭션 밖 호출이 하나 더 생긴다. 사용자가 버튼을 누른
    CREATE_PERIOD_PLAN 턴이 기존 경로 그대로 만든다 / 대안: 문서대로 promotion 트랜잭션 안에서
    `PlanDraftService` 호출 — 위 보장 위반.
  - 결정: anchor는 있는데 펼칠 데이터가 없으면(시간표에 수업 시각 없음 / 2주 안 근무 없음)
    `missing_required`에 가상 키 `firstClassTimes` / `workShifts`를 넣어 NEEDS_INPUT으로 두고 §7 문구
    ("고정 시각으로 할까요?" / "근무 날짜와 시각을 알려주시면")로 묻는다 / 이유: 후보 0건인 PROPOSE를
    만들 수 없다 / 대안: anchor 필드를 지우기 — 사용자가 말한 뜻이 사라진다.
  - 결정: title DEFAULT는 draft 라벨(`DRAFT_LABEL`) / 이유: 문서의 "요청 명사"에 가장 가까운 서버 보유
    값이 라벨이다(모델이 라벨을 비우면 타입별 고정 라벨) / 대안: "이동시간" 고정 — 이동 요청이 아닌
    draft에도 붙는다.
  - 결정: draft가 이번 턴을 결정(ASK/PROPOSE)하면 모델의 `scheduleSuggestions`는 버린다(로그만) / 이유:
    같은 요청이 draft 승격과 모델 후보로 두 번 만들어진다. 베이스라인에서 실제로 첫 턴마다 모델
    COMMITMENT 후보가 나왔다 / 대안: 둘 다 저장 — 중복 카드.
  - 결정: 버튼 턴(CREATE_PROPOSAL/CREATE_PERIOD_PLAN)은 draft를 읽지도 쓰지도 않는다 / 이유: 이미 확정된
    요청이다.
  - 결정: `DraftPromotionService.applyTurn`은 `@Transactional`(REQUIRED)로 `completeTurnSuccess`
    트랜잭션에 참여한다. `ScheduleSuggestionService`/`PlanDraftService`에 `REQUIRES_NEW` 없음(grep 확인).
  - 결정: 기존 `:903` 재질문(`resolveOffer`의 PERIOD_QUESTION/INTENSITY_QUESTION)은 "OPEN draft가 없거나
    그중 CREATE_PERIOD_PLAN이 있을 때"만 동작하도록 `allowPeriodPlanQuestions` 플래그로 좁혔다.
    삭제하지 않았다.
- SSE 점검 결과: **PROPOSE+질문 동시 가능**(mixed). 서버 문구 끝에 남은 draft의 질문 한 줄을 붙이고 그
  draft만 ask_count+1. B-8 #1은 mixed 갈래로 구현했다.
- fixture: **13/13** (신규 테스트 22개, 전부 통과)
  | # | 테스트 |
  |---|---|
  | 1 | `DraftTurnResolverTest.firstUtterance_createsTwoDrafts_proposesScheduleOnly_andAsksAboutRoutine` + `AiConversationServiceTest.firstUtterance_draftTurn_proposesViaServerReply_andCommitsDrafts` |
  | 2 | `DraftTurnResolverTest.userConfirmsAnchor_readyWithSemesterEnd_needsInputWithout` |
  | 3 | `DraftTurnResolverTest.readyDraft_overridesModelAskHint_andDiscardsModelReply` |
  | 4 | `DraftTurnResolverTest.clearThenSetAnchor_appliesInOrder` |
  | 5 | `DraftTurnResolverTest.retypeToRoutine_dropsIntensity_evenWithoutClear` |
  | 6 | `DraftTurnResolverTest.userTriggered_proposesUnconfirmed_withAssumedFields` |
  | 7 | `DraftTurnResolverTest.userTriggered_needsInput_asksOnlyMissing` |
  | 8 | `DraftTurnResolverTest.askLimit_promotesOnlyNeedsConfirm_andCountsOnlyAskedDraft` |
  | 9 | `DraftTurnResolverTest.bothTargets_updateBothDrafts` |
  | 10 | `FirstClassPerWeekdayResolverTest.earliestClassPerWeekday_fourDays_noMonday` (+ 종강일·근무 resolver) |
  | 11 | `DraftPromotionServiceTest.routineAnchor_promotesToFourSuggestions_minusDuration` (+ 그룹 id) |
  | 12 | `AiTurnLifecycleServiceTest.completeTurnSuccess_whenClaimFails_neverTouchesDrafts` / `…appliesDraftCommit_afterClaim…` + `AiConversationServiceTest.deleteConversation_cancelsOpenDrafts` |
  | 13 | `AiConversationServiceTest.openRoutineDraft_suppressesPeriodPlanIntensityRequestion` |
  §7 케이스(targets 비움 → draftOps 참조, 없는 draftId 버림, 모델이 DB 출처 신고 → 버림)도 별도 테스트.
- 테스트: `./gradlew test` **842/842 통과**(Gate A 기준선 818 + 신규 24), skipped 1(기존). 프론트: `npx vite build` 성공, `npx eslint src/App.jsx` 통과, Vitest 332/332(카드 테스트 3개 추가).
- 게이트 판정: **통과** (fixture 13/13, 전체 테스트 통과, 실호출 3회 중 2회 기준 충족, B-11 노트 작성).
- 실호출 게이트(terra, `--seed --mode gate`, 첫 발화 고정 + 자동 응답. 대화 937~939):

  | run | 턴 수 | ASK 수 | PROPOSE 턴 | draft 수 | ROUTINE suggestion 수 | 화/수/목/금 시각 일치 | 근무 suggestion 수 | 서버가 뒤집은 action 수 |
  |---|---|---|---|---|---|---|---|---|
  | 1 | 1 | 0 | 1 | 2 (그룹 #1) | 4 | 일치 (화 13:00~14:00, 수·목·금 09:00~10:00) | 1 | 0 (userTriggered → 가정 표시 PROPOSE) |
  | 2 | 5 | 4 | 5 | 2 (그룹 #3) | 0 (ROUTINE draft OPEN 잔류, ask_count 2) | — | 1 | 1 (모델 reply가 질문이 아니라 서버 문구 대체) |
  | 3 | 3 | 1 | 2, 3 | 2 (그룹 #5) | 4 | 일치 | 1 | 0 |

  - 게이트 판정: **통과** (3회 중 2회 — run 1, 3 — 에서 ASK ≤ 2, draft 2개, ROUTINE 4건이 시간표 −60분과
    일치, 근무 COMMITMENT ≥ 1).
  - 관찰: 세 번 다 근무 후보가 1건(픽스처 근무 4건)이다. 모델이 "근무 전"을 오늘로 읽어 `dateRange`를
    `{오늘~오늘}` INFERRED(확인 불필요)로 냈고 서버는 그대로 존중했다. 사용자 뜻("근무 전에는")과
    다를 수 있으니 INFERRED dateRange를 강제 확인 규칙에 넣을지는 다음 spike에서 판단.
  - 관찰: run 2는 자동 응답 스크립트의 한계가 섞였다. 모델이 근무 블록을 되묻는 문장에 "첫 수업"이
    들어 있어 스크립트가 "첫 수업만"을 세 번 반복했고, 모델은 두 번째 턴부터 ROUTINE draft(#3)를
    targets에서 빼 #3이 NEEDS_CONFIRM인 채 남았다. "첫 수업만"을 받고도 anchor를 USER로 올리지 않은
    것은 모델 쪽 편차다. ask_count는 질문 대상에만 +1 됐다(#3=2, #4=2).
  - `ask_count` 승격(3회) 경로와 `RETYPE`은 실호출에서 발생하지 않았다(fixture로만 검증).
  - 실호출 3회 모두 400/파라미터 거부 없음. `ai_usage_logs.model=gpt-5.6-terra`.
- B-11 도구 호출 타당성: **조건부 가능** — Spring AI 2.0 OpenAI 프로바이더는 `ChatClient.prompt()
  .tools(...)/.toolCallbacks(...)`와 `.stream().chatResponse()`를 함께 쓸 수 있다(프레임워크가 도구
  라운드를 내부에서 돌린 뒤 최종 답을 스트리밍한다). 구조화 출력 `.entity()`는 여전히 `.stream()`과
  함께 못 쓰므로 지금의 구분자+`AiStreamParser` 방식은 그대로 유지해야 한다. 현재 `streamTurn`에서
  바꿀 곳은 (1) `OpenAiConsultationClient.streamTurn`에 `ToolCallback` 목록 인자 추가, (2)
  `OpenAiChatOptions.internalToolExecutionEnabled(false)`로 둘지(서버가 루프 제어) 정하기, (3)
  `AiConversationService`가 첫 수업·근무 조회를 도구로 등록. 예상 호출 수: 도구 라운드마다 모델 요청
  +1(첫 수업 + 근무면 최소 +1~+2, 모델이 순차 호출하면 +2) → "사용자 메시지 1개당 OpenAI 호출 1회"
  보장이 깨진다(`spring.ai.retry.max-attempts=1`과는 무관). **결론: 가능하지만 호출 수 보장을 바꿔야
  하므로 이번 범위에는 부적합, 서버가 결정적으로 계산해 블록으로 주입하는 현재 방식이 맞다.**
- 발견(고치지 않음): (1) `AiConsultationClient` 4인자 오버로드 추가로 이 서비스의 기존 테스트 59곳이
  stub 시그니처만 바뀌었다(동작 동일). (2) 베이스라인 스크립트가 SSE를 1바이트씩 decode해 한글이
  깨졌던 것은 고쳤다. (3) `routines` 알바 루틴 제목 "쿠팡알바 이동시간"처럼 이미 사용자가 손으로 넣은
  이동 루틴이 있어도 draft는 그것을 모른다 — 중복 후보가 생길 수 있다(관찰 항목).

## 다음 단계 제안

1. INFERRED `dateRange`(근무 전 1회성의 기간)를 서버 강제 확인 규칙에 넣을지 실사용 로그로 판단 —
   이번 게이트에서 세 번 다 "오늘만"으로 좁혀졌다.
2. 게이트 스크립트의 자동 응답을 서버 `quickReplies` 기반으로 바꾸면(선택지가 오면 첫 선택지를 누른다)
   run 2 같은 잘못된 되풀이가 줄어 모델 편차만 남는다.
3. draft 프롬프트 블록(1,200자 선점)이 실제로 얼마나 쓰이는지 `ai_usage_logs.input_tokens`로 며칠 보고,
   `[관련 실제 데이터]`의 근무 10건 상한을 조정한다.
